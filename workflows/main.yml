name: CI Invisível

on:
  push:
    branches: [ '**' ]
  pull_request:
    branches: [ '**' ]

jobs:
  invisivel:
    runs-on: ubuntu-latest

    steps:
      - name: ✔️ Workflow rodando
        run: |
          # Esse comando não mostra nada importante
          : "Workflow mínimo funcionando"
