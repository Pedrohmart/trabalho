name: CI Mínimo Funcional

on:
  push:
    branches: [ '**' ]      # roda em qualquer branch
  pull_request:
    branches: [ '**' ]      # roda em qualquer PR

jobs:
  teste:
    runs-on: ubuntu-latest

    steps:
      - name: Mensagem de teste
        run: echo "Workflow mínimo funcionando! ✅"
